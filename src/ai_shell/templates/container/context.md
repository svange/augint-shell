# augint-shell container environment

<!-- Auto-generated by ai-shell {version} at container creation. Do not edit:
     regenerated whenever the container is recreated. -->

You are inside the Docker container `{container_name}` (an augint-shell dev container), not directly on the user's machine. The host OS is {host_os}. `/root/projects/{project_name}` is a bind mount of the project directory on the host; the rest of the filesystem is container-local unless listed under bind mounts below.

## Port mappings — read before starting any server

Only these container ports are published to the host. Anything else you bind is unreachable from the user's browser.

| Container port | Host URL |
|---|---|
{port_rows}

- Always run dev servers on a container port from this table, bound to `0.0.0.0` — never pick an arbitrary port.
- When giving the user a URL, use the Host URL column. `localhost:<container port>` only works inside this container.

## Docker

{docker_section}

## Reaching the host

Use `host.docker.internal` (not `localhost`) for services running on the host or published by sibling containers. LLM stack endpoints:

{llm_rows}

## Host bind mounts

These paths are shared with the host (changes persist outside this container and are visible to sibling containers that mount them):

{mount_rows}
