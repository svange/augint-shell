gate: Security
language: python
repo_type: iac
required_steps:
  - kind: action
    matches: actions/checkout
  - kind: action
    matches: actions/setup-python
  - kind: action
    matches: astral-sh/setup-uv
  - kind: run
    matches_regex: 'bandit'
  - kind: run
    matches_regex: 'pip-audit'
  - kind: action
    matches: semgrep/semgrep-action
