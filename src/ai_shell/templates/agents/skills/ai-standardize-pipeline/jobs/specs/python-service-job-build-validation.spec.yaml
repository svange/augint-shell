gate: Build validation
language: python
repo_type: service
required_steps:
  - kind: action
    matches: actions/checkout
  - kind: action
    matches: actions/setup-python
  - kind: action
    matches: astral-sh/setup-uv
  - kind: run
    matches_regex: '(sam (validate|build)|cdk synth|terraform (validate|plan))'
