# GitHub token for semantic release (needs contents:write on this repo)
GH_TOKEN=

# Docker Hub credentials for image publishing
DOCKERHUB_USERNAME=
DOCKERHUB_TOKEN=

# Optional: Override Python version for CI (default: 3.12)
# PYTHON_VERSION=3.12
